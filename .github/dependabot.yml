# Keeps the SHA-pinned actions in .github/workflows up to date.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    # Wait out brand-new releases before proposing them (supply-chain caution).
    cooldown:
      default-days: 7
